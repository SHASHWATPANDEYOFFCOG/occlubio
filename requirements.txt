# Core + inference (baseline pipeline). Prefer:  pip install -e ".[infer]"
numpy>=1.23
opencv-python>=4.7
pyyaml>=6.0
tqdm>=4.65
insightface>=0.7.3
onnxruntime>=1.16        # CUDA box: replace with onnxruntime-gpu
faiss-cpu>=1.7.4         # server/DGX: replace with faiss-gpu
